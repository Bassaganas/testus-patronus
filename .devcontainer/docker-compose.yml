version: '3.8'

services:
  app:
    build:
      context: ..
      dockerfile: backend/Dockerfile
      target: development
    volumes:
      - ..:/workspace:cached
    command: sleep infinity
    network_mode: service:chroma

  chroma:
    image: chromadb/chroma:latest
    restart: unless-stopped
    volumes:
      - chroma_data:/chroma/chroma
    environment:
      - ALLOW_RESET=true
      - ANONYMIZED_TELEMETRY=false

volumes:
  chroma_data:
